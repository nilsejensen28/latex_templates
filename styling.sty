%This file contains the package "styling" which provides the commands to style the document.

\ProvidesPackage{styling}

\RequirePackage[nswissgerman]{babel} %Todo: Add a language toggle
\RequirePackage{fix-cm}
\RequirePackage[T1]{fontenc}
\RequirePackage[utf8]{inputenc}
\RequirePackage[sfdefault,light]{FiraSans} %Use the font Fira Sans

\RequirePackage{calc} %Allows for computation when setting lengths

\RequirePackage{enumitem} %Allows for customizing lists